{% extends 'Front.html.twig' %}

{% block title %}Edit Event{% endblock %}

{% block body %}
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <h2 class="mt-4 mb-4 text-center">Editer un evenement </h2>
    <div class="container mt-4"> <!-- Ajout d'un conteneur pour centrer le contenu -->
        <div class="row justify-content-center"> <!-- Centrer le contenu horizontalement -->
            <div class="col-md-6">
                <link rel="shortcut icon" type="image/png" href="{{ asset('assets/images/logos/favicon.png') }}">
                <!-- Update CSS path -->
                <link rel="stylesheet" href="{{ asset('assets/css/styles.min.css') }}">
                {% block stylesheets %}
                    {{ encore_entry_link_tags('app') }}
                {% endblock %}
    { include('event/_form.html.twig', {'button_label': 'Update'}) }    <a href="{{ path('app_event_index') }}">back to list</a>

    {{ include('event/_delete_form.html.twig') }}
      </div>
        </div>
    </div>
{% endblock %}
