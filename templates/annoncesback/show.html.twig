{% extends 'Back.html.twig' %}

{% block title %}Annonces{% endblock %}

{% block body %}
    <h1></h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ annonce.titre }}</td>
            </tr>
            <tr>
                <th>Datedepub</th>
                <td>{{ annonce.datedepub ? annonce.datedepub|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ annonce.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_annoncesback_index') }}">retourner aux liste</a>

    

    {{ include('annoncesback/_delete_form.html.twig') }}
{% endblock %}
