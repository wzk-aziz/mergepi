{% extends 'Back.html.twig' %}

{% block title %}Commnet{% endblock %}

{% block body %}
    <h1></h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commnet.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commnet.contenu }}</td>
            </tr>
            <tr>
                <th>Datecommnt</th>
                <td>{{ commnet.datecommnt ? commnet.datecommnt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commnetback_index') }}">back to list</a>

    

    {{ include('commnetback/_delete_form.html.twig') }}
{% endblock %}
